services:
  app:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: culinary_app
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/var/www
      - ./docker/php/php.ini:/usr/local/etc/php/conf.d/custom.ini
    networks:
      - culinary_net
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  postgres:
    image: postgres:16-alpine
    container_name: culinary_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_DATABASE:-culinary_db}
      POSTGRES_USER: ${DB_USERNAME:-culinary_user}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-secret}
    ports:
      - "5111:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - culinary_net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-culinary_user} -d ${DB_DATABASE:-culinary_db}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: culinary_redis
    restart: unless-stopped
    ports:
      - "6100:6379"
    volumes:
      - redis_data:/data
    networks:
      - culinary_net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  culinary_net:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
